A website to calculate the shortest path and its distance between two planets.

To start up the front end 

cd holigram
ng serve

To Start up the backend 
cd nuclearFissionCore
./mvnw clean package && java -jar target/*.jar